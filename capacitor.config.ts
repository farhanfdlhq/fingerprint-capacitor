import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fingerprintcapacitor.app',
  appName: 'fingerprint-capacitor',
  webDir: 'dist'
};

export default config;
